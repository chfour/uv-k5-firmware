sources += files(
    'board.c',
    'dcs.c',
    #'frequencies.c',
    'init.c',
    'main.c',
    'misc.c',
    'scheduler.c',
    'version.c'
)

includes += include_directories('.')

subdir('app')
subdir('ui')
